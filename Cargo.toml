[package]
name = "Solana-CLI-Wallet"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
clap = {version = "4.5.43", features = ["derive"]}
tokio = {version = "1.47.1", features = ["full"]}
